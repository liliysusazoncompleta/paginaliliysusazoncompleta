# Esta carpeta ya no se usa

Las imagenes de producto se movieron a **`backend/public/productos/`**, para que
funcionen igual en desarrollo y en produccion sin depender de un rebuild del
frontend. Coloca ahi tus fotos y sigue las instrucciones del `README.md` de esa
carpeta.

Cualquier archivo que dejes aca (en `frontend/public/productos/`) no se usara.
