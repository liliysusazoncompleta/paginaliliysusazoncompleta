providers = ["node"]

[phases.setup]
nixPkgs = ["nodejs_20"]

[phases.install]
cmds = [
  "npm install -g pnpm@10.12.1",
  "pnpm install --frozen-lockfile"
]

[phases.build]
cmds = ["pnpm --filter @lili/backend build"]

[start]
cmd = "pnpm --filter @lili/backend start"
